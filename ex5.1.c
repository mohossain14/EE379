//prints decimal to binary bit

#include <stdio.h>
#include <math.h>

int main(void){
    int dig, sub, i, k;
    int max;

    printf("Enter a number: ");
    scanf("%d", &dig);

    for (i = 0; i < 200; i++){
        if (pow(2, i) > dig){
            max = pow(2, i );
        }
    }
    k = 
}